# ==============================================================================
# your-domain.key.example — Cloudflare Origin CA 私钥示例
# ==============================================================================
#
# 此文件是占位符，用于说明此目录需要放置的私钥文件格式。
# 真实私钥文件名格式：your-domain.com.key（不带 .example 后缀）
#
# 🔑 获取方式：
#   私钥在 Cloudflare 控制台创建 Origin CA 证书时同步生成（只显示一次！）
#   下载 .key 文件，保存到此目录，例如：chonkybird.com.key
#
# 🚨 安全要求（极其重要）：
#   - 私钥文件（.key）绝对【禁止】提交到任何 Git 仓库（公开或私有均不行！）
#   - 已在 .gitignore 中排除：caddy/certs/*.key
#   - 服务器上的私钥文件权限必须设置为 600：
#     chmod 600 /opt/personal-blog-deploy/caddy/certs/*.key
#   - 私钥泄漏后必须立即到 Cloudflare 控制台撤销（Revoke）证书
#
# 真实文件内容示例（结构如下）：
# -----BEGIN PRIVATE KEY-----
# MIIEpDCCAowCCQD...（base64 编码的私钥内容）
# -----END PRIVATE KEY-----

# ==============================================================================
# your-domain.pem.example — Cloudflare Origin CA 证书（公钥）示例
# ==============================================================================
#
# 此文件是占位符，用于说明此目录需要放置的证书文件格式。
# 真实证书文件名格式：your-domain.com.pem（不带 .example 后缀）
#
# 🔐 如何获取 Cloudflare Origin CA 证书：
#   1. 登录 Cloudflare 控制台 → 选择你的域名
#   2. SSL/TLS → Origin Server → Create Certificate
#   3. 选择证书有效期（最长 15 年）
#   4. 下载 .pem 文件（公钥证书），保存到此目录
#   5. 将文件名改为你的域名，例如：chonkybird.com.pem
#
# ⚠️  注意事项：
#   - 此文件（.pem）是公钥证书，可以公开，但仍建议不提交 Git
#   - 私钥文件（.key）绝对不能提交 Git！
#   - Cloudflare SSL/TLS 模式必须设为 "Full (Strict)"，Origin CA 才能生效
#
# 参考文档：https://developers.cloudflare.com/ssl/origin-configuration/origin-ca/
#
# 真实文件内容示例（结构如下）：
# -----BEGIN CERTIFICATE-----
# MIIEpDCCAowCCQD...（base64 编码的证书内容）
# -----END CERTIFICATE-----
